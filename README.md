# Osint_Cl

Es un proyecto para buscar rut y otras cosas interesantes

## Instalación

Para ejecutar este proyecto, asegúrate de tener Python instalado. Además, necesitarás instalar las siguientes bibliotecas utilizando `pip`:

```bash
pip install termcolor
pip install requests
pip install beautifulsoup4
